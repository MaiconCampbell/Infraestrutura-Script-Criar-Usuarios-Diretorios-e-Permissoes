 Todo script inicia com !/bin/bash e precisar ter a extensão .sh
  A execução do script deve ser realizado por usuário ROOT
  Antes de executar o script rodar o comando chmod +x NomeScript.sh (permite a execução do .sh)